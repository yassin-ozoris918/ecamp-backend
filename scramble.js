const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const deletedUsers = await prisma.user.findMany({ where: { deletedAt: { not: null }, email: { not: { contains: '_deleted_' } } } }); for (const user of deletedUsers) { await prisma.user.update({ where: { id: user.id }, data: { email: user.email + '_deleted_' + Date.now() } }); console.log('Scrambled email for deleted user:', user.email); } console.log('Done!'); process.exit(0); } main().catch(console.error);
